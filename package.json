{
  "name": "proofship",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI release agent that never says shipped until production proves it.",
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/agent.mjs && node --check src/integrations.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
